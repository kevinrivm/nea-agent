# Versiones exactas: lo que se probó es lo que se despliega. Con rangos, cada
# build del Dockerfile podía traer una versión distinta de la que corrió la
# suite (y un major nuevo del SDK del modelo o de asyncpg entraba solo).
# Para subir una: cambiarla aquí, correr pytest (y el job de Postgres) y
# commitear las dos cosas juntas.
fastapi==0.141.1
uvicorn[standard]==0.53.0
httpx==0.28.1
asyncpg==0.30.0
openai==2.54.0
pydantic-settings==2.15.0
tzdata==2026.4
pypdf==6.19.0
